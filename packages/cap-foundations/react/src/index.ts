/**
 * @fluentui-contrib/cap-foundations-react
 *
 * React theme provider and components built on cap-foundations design tokens.
 */

// Theme provider + hook
export { ThemeProvider, useTheme } from './context/ThemeProvider';
export type { ThemeProviderProps } from './context/ThemeProvider';
