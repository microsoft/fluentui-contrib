import { Meta } from '@storybook/react';
export { Default } from './Default.stories';

const meta: Meta = {
  title: 'Resize Handle',
  parameters: {
    layout: 'fullscreen',
  },
};

export default meta;
