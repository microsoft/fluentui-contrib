import React from 'react';
import { makeStyles } from '@material-ui/core/styles'; // or '@mui/styles' for MUI v5

const useStyles = makeStyles({
  // your styles here
});

const SearchBoxExample = () => {
  const classes = useStyles();
  return (
    // your component JSX
  );
};

export default SearchBoxExample;
