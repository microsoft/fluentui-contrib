import { Meta } from '@storybook/react';
import { Controls } from '@fluentui-contrib/azure-theme';

export { Default } from './Default.stories';

const meta: Meta<typeof Controls> = {
  component: Controls, 
};

export default meta;