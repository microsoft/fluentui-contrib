import * as React from 'react';
import { Default } from './Default.stories';

export const MultipleGroups = () => {
  return (
    <>
      <Default></Default>
      <Default></Default>
      <Default></Default>
    </>
  );
};
