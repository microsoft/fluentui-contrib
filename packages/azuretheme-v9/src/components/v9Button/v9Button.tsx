import * as React from 'react';
import { Button } from '@fluentui/react-components';
import type { ButtonProps } from '@fluentui/react-components';

export const v9Button = (props: ButtonProps) => <Button />;
