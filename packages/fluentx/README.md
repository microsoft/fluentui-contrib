# fluentx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fluentx` to build the library.

## Running unit tests

Run `nx test fluentx` to execute the unit tests via [Jest](https://jestjs.io).
