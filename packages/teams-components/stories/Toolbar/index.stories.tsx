import type { Meta } from '@storybook/react';
import { Toolbar } from '@fluentui-contrib/teams-components';
export { Default } from './Default.stories';

const meta = {
  title: 'Packages/teams-components/Toolbar',
  component: Toolbar,
} satisfies Meta<typeof Toolbar>;

export default meta;
