import { Meta } from '@storybook/react';
import { variantThemeTool } from '@fluentui-contrib/variant-theme';
export { Default } from './Default.stories';

const meta: Meta<typeof variantThemeTool> = {
  component: variantThemeTool,
};

export default meta;
