import * as React from 'react';
import { Meta } from '@storybook/react';
import { AccessibleChat } from '@fluentui-contrib/accessible-chat';
export { Default } from './Default.stories';

const meta: Meta<typeof AccessibleChat> = {
  component: AccessibleChat,
};

export default meta;
